I am a GUI client for BooksCollector application.

I house  all UI components.

I need all *UI components to work. I create them in init methods.

Open me with "BooksCollectorClient new open" .Then connect me to a link (such as localhost:8080).